This is a ticketing system application.
Python3 has been used as the programming language for this application, and vs code has been used as the main IDE while using Python3.  
A total of 4 classes will be added to the application: 
1.    Main: the main class of the application that will contain the main method which connects the whole application together and shows the menu. 
2.    Ticket: the ticket class contains all the required attributes of the ticket.
3.    PasswordGenerator: this class will contain all the methods that will be needed to be used to generate a new password for the user. 
4.    TicketStats: this class will contain the method that will return the statistics of the tickets created. 


