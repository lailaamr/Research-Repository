To run Python scripts with the python command, 
one need to open a command-line (terminal or CMD) and type in the word python, or python3 if they have both versions, 
followed by the path to the script named main.py
